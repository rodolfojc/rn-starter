import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

const BoxScreen = () => {
    return (
        <View>
            <Text>Box Layout</Text>
        </View>
    );
};

const styles = StyleSheet.create({});

export default BoxScreen;